Explanation:

myMap is added to Array.prototype so it can be used on any array.
The method iterates over the array, applying the callback to each element.
Handles the optional thisArg parameter.
Checks for i in this to handle sparse arrays (arrays with missing elements).